# CNFast 构建配置模板
# 复制本文件为 .env 并填入真实值后构建：
#   cp .env.example .env
# 注意：.env 已被 .gitignore 忽略，请勿提交真实密钥。

# API 请求加密密钥（必须成对设置，长度需满足 AES 要求）
AESKEY=
AESIV=

# 打包产物命名（Makefile 的 APP_NAME 可在下方覆盖）
APP_NAME=cnfast
APP_VERSION=1.2.0
